/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/*!
 * \file act_ulq_clamp_max_grad_infershape.cpp
 * \brief ActULQClampMaxGrad 算子的 output shape / dtype 推理。
 *
 *   - 全轴归约（axis_source: implicit_all）→ 输出恒为 0 维标量 shape=()（keep_dims 不涉及）
 *   - unknown rank 透传：输入未知 → 输出标 unknown rank（保留必读文件透传守卫）
 *   - 输出 dtype 跟随 y_grad（input0）
 *   - 校验三输入 shape 完全相同（y_grad == clamp_max_mask == x_clamped_loss）
 *   - 空 tensor 不在 infershape 端短路：output shape 天然是 0 维标量，由 tiling/kernel 端处理 0 元素输入
 */

#include "register/op_impl_registry.h"
#include "exe_graph/runtime/infer_shape_context.h"
#include "op_common/log/log.h"

using namespace ge;

namespace ops {

namespace {
constexpr size_t kInputYGradIdx = 0;
constexpr size_t kInputMaskIdx = 1;
constexpr size_t kInputXLossIdx = 2;
constexpr size_t kOutputIdx = 0;
constexpr int64_t kUnknownRankDim = -2;
} // namespace

static bool IsUnknownRank(const gert::Shape& shape)
{
    return shape.GetDimNum() == 1 && shape.GetDim(0) == kUnknownRankDim;
}

static void SetUnknownRank(gert::Shape& shape)
{
    shape.SetDimNum(0);
    shape.AppendDim(kUnknownRankDim);
}

static bool ShapeEqual(const gert::Shape& a, const gert::Shape& b)
{
    if (a.GetDimNum() != b.GetDimNum()) {
        return false;
    }
    for (size_t i = 0; i < a.GetDimNum(); ++i) {
        if (a.GetDim(i) != b.GetDim(i)) {
            return false;
        }
    }
    return true;
}

static ge::graphStatus InferShape4ActULQClampMaxGrad(gert::InferShapeContext* context)
{
    const gert::Shape* yGradShape = context->GetInputShape(kInputYGradIdx);
    OP_CHECK_NULL_WITH_CONTEXT(context, yGradShape);
    const gert::Shape* maskShape = context->GetInputShape(kInputMaskIdx);
    OP_CHECK_NULL_WITH_CONTEXT(context, maskShape);
    const gert::Shape* xLossShape = context->GetInputShape(kInputXLossIdx);
    OP_CHECK_NULL_WITH_CONTEXT(context, xLossShape);

    gert::Shape* outShape = context->GetOutputShape(kOutputIdx);
    OP_CHECK_NULL_WITH_CONTEXT(context, outShape);

    // unknown rank 透传：任一输入未知 → 输出标未知
    if (IsUnknownRank(*yGradShape) || IsUnknownRank(*maskShape) || IsUnknownRank(*xLossShape)) {
        SetUnknownRank(*outShape);
        return ge::GRAPH_SUCCESS;
    }

    // 输入 rank 上限 8（不支持 9 维及以上 Tensor）
    OP_CHECK_IF(yGradShape->GetDimNum() > 8,
                OP_LOGE(context, "ActULQClampMaxGrad: input rank %zu exceeds 8 (9D+ tensor not supported)",
                        yGradShape->GetDimNum()),
                return ge::GRAPH_FAILED);

    // 校验三输入 shape 完全相同（broadcast.kind: none）
    OP_CHECK_IF(!ShapeEqual(*yGradShape, *maskShape) || !ShapeEqual(*yGradShape, *xLossShape),
                OP_LOGE(context, "ActULQClampMaxGrad: three inputs must have identical shape (no broadcast)"),
                return ge::GRAPH_FAILED);

    // 全轴归约 → 输出 0 维标量 shape=()
    outShape->SetDimNum(0);
    return ge::GRAPH_SUCCESS;
}

IMPL_OP_INFERSHAPE(ActULQClampMaxGrad).InferShape(InferShape4ActULQClampMaxGrad);

} // namespace ops
