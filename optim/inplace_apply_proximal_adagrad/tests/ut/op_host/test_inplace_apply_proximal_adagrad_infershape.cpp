/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file test_inplace_apply_proximal_adagrad_infershape.cpp
 * \brief InplaceApplyProximalAdagrad InferShape 独立逻辑测试
 *
 * 测试策略：从 inplace_apply_proximal_adagrad_infershape.cpp 复制 static 函数逻辑，
 * 不依赖 CANN faker 框架，使用独立编译验证形状推导正确性。
 *
 * InferShape 逻辑：双输出 inplace 操作
 *   - output var (index 0).shape = input var (index 0).shape
 *   - output accum (index 1).shape = input accum (index 1).shape
 *   - output var.dtype = input var.dtype
 *   - output accum.dtype = input var.dtype
 */

#include <gtest/gtest.h>
#include <cstdint>
#include <vector>
#include <string>
#include <utility>

// ===================== 被测逻辑（从源文件复制） =====================

using Shape = std::vector<int64_t>;

enum LocalDataType {
    DT_FLOAT = 0,
    DT_FLOAT16 = 1,
    DT_BF16 = 3,
};

static constexpr size_t VAR_INDEX = 0;
static constexpr size_t ACCUM_INDEX = 1;

/*!
 * \brief InferShape4InplaceApplyProximalAdagrad 逻辑复制
 *
 * 原始逻辑（inplace_apply_proximal_adagrad_infershape.cpp）：
 *   const gert::Shape* varShape = context->GetInputShape(VAR_INDEX);
 *   gert::Shape* varOutShape = context->GetOutputShape(VAR_INDEX);
 *   *varOutShape = *varShape;
 *   const gert::Shape* accumShape = context->GetInputShape(ACCUM_INDEX);
 *   gert::Shape* accumOutShape = context->GetOutputShape(ACCUM_INDEX);
 *   *accumOutShape = *accumShape;
 *
 * 双输出 inplace 操作：var 输出形状等于 var 输入形状，accum 输出形状等于 accum 输入形状
 */
static std::pair<Shape, Shape> InferShape4InplaceApplyProximalAdagrad(const Shape& inputVarShape,
                                                                      const Shape& inputAccumShape)
{
    Shape outputVarShape = inputVarShape;
    Shape outputAccumShape = inputAccumShape;
    return {outputVarShape, outputAccumShape};
}

/*!
 * \brief InferDataType4InplaceApplyProximalAdagrad 逻辑复制
 *
 * 原始逻辑：
 *   const ge::DataType dtype = context->GetInputDataType(VAR_INDEX);
 *   context->SetOutputDataType(VAR_INDEX, dtype);
 *   context->SetOutputDataType(ACCUM_INDEX, dtype);
 *
 * 双输出 dtype 推导：两个输出 dtype 均与输入 var 相同
 */
static std::pair<LocalDataType, LocalDataType> InferDataType4InplaceApplyProximalAdagrad(LocalDataType inputVarDtype)
{
    return {inputVarDtype, inputVarDtype};
}

// ===================== 测试用例 =====================

TEST(InplaceApplyProximalAdagradInferShape, infershape_basic_2d)
{
    Shape varShape = {8, 16};
    Shape accumShape = {8, 16};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar.size(), varShape.size());
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum.size(), accumShape.size());
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_1d)
{
    Shape varShape = {256};
    Shape accumShape = {256};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_3d)
{
    Shape varShape = {4, 1, 8};
    Shape accumShape = {4, 1, 8};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_4d)
{
    Shape varShape = {2, 3, 4, 8};
    Shape accumShape = {2, 3, 4, 8};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_5d)
{
    Shape varShape = {1, 2, 3, 4, 5};
    Shape accumShape = {1, 2, 3, 4, 5};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_empty_tensor)
{
    Shape varShape = {0};
    Shape accumShape = {0};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_empty_mid_dim)
{
    Shape varShape = {2, 0, 3};
    Shape accumShape = {2, 0, 3};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_large)
{
    Shape varShape = {1024, 2048};
    Shape accumShape = {1024, 2048};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_single_elem)
{
    Shape varShape = {1};
    Shape accumShape = {1};
    auto [outVar, outAccum] = InferShape4InplaceApplyProximalAdagrad(varShape, accumShape);
    EXPECT_EQ(outVar, varShape);
    EXPECT_EQ(outAccum, accumShape);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_fp32_dtype)
{
    auto [outVarDtype, outAccumDtype] = InferDataType4InplaceApplyProximalAdagrad(DT_FLOAT);
    EXPECT_EQ(outVarDtype, DT_FLOAT);
    EXPECT_EQ(outAccumDtype, DT_FLOAT);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_fp16_dtype)
{
    auto [outVarDtype, outAccumDtype] = InferDataType4InplaceApplyProximalAdagrad(DT_FLOAT16);
    EXPECT_EQ(outVarDtype, DT_FLOAT16);
    EXPECT_EQ(outAccumDtype, DT_FLOAT16);
}

TEST(InplaceApplyProximalAdagradInferShape, infershape_bf16_dtype)
{
    auto [outVarDtype, outAccumDtype] = InferDataType4InplaceApplyProximalAdagrad(DT_BF16);
    EXPECT_EQ(outVarDtype, DT_BF16);
    EXPECT_EQ(outAccumDtype, DT_BF16);
}
